# renaming file from specific no

cd Downloads/Telegram\ Desktop/
ls
# a=300
# for i in *.jpg; do
#   new=$(printf "%01d.jpg" "$a") #04 pad to length of 4
#   mv -i -- "$i" "$new"
#   a=$(( $a + 1 ))
# done